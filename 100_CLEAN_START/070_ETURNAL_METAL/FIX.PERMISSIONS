

chown 9000:9000 eturnal.yml

cp -fr ~/004_ACME_CERTS/certificates ACME_LEGO/
chown -R 9000:9000 ACME_LEGO
chmod 700 ACME_LEGO
chmod 600 ./ACME_LEGO/*

mkdir ./log_dir
mkdir ./run_dir

chown -R 9000:9000 log_dir run_dir
chmod -R 744 log_dir run_dir
