# Configuration file for Synapse.
#
# This is a YAML file: see [1] for a quick introduction. Note in particular
# that *indentation is important*: all the elements of a list or dictionary
# should have the same indentation.
#
# [1] https://docs.ansible.com/ansible/latest/reference_appendices/YAMLSyntax.html
#
# For more information on how to configure Synapse, including a complete accounting of
# each option, go to docs/usage/configuration/config_documentation.md or
# https://element-hq.github.io/synapse/latest/usage/configuration/config_documentation.html
server_name: "matrix.landingdev.xyz"
pid_file: /data/homeserver.pid
listeners:
  - port: 8008
    tls: false
    type: http
    x_forwarded: true
    resources:
      - names: [client, federation]
        compress: false

# database:
#   name: sqlite3
#   args:
#     database: /data/homeserver.db

database:
  name: psycopg2
  args:
    user: synapse
    password: synapse
    database: synapse
    host: synapse_db
    cp_min: 5
    cp_max: 10

log_config: "/data/matrix.landingdev.xyz.log.config"
media_store_path: /data/media_store
registration_shared_secret: "@SdvkHr6ewfE^Dc=f&l7_KIZv2vF3DcVME3X*m-it#LD12DNS7"
report_stats: true
macaroon_secret_key: "3*rwdETB=MJzg1z8Fz1At5,@n97Vic;=eo^+&OyDXeoUYf-^bg"
form_secret: "dg-_suo_;ww-K^y:-w2_:toujxdb9_NseRjE&fkmDCDs_Esg0H"
signing_key_path: "/data/matrix.landingdev.xyz.signing.key"
trusted_key_servers:
  - server_name: "matrix.org"


# vim:ft=yaml
